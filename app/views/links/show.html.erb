<h1>Mood:</h1>
<h3><%= @link.title %></h1>
<h2>Description: <%= @link.url %></h2>
  <hr>
<h3>Times that <%= current_user.username %> has felt <%= @link.title %></h3>

<!-- want a link_to new article here -->
<%= link_to 'Create Article', new_link_article_url(@link) %>


<!-- want a list of all associated articles here -->

<ul>
  <% @link.articles.each do |article| %>
  <li>
    <%= link_to article.body.split[0..6].join(' ') , article_url(article)%>
  </li>
  <% end %>
</ul>
